# 🌟 دليل احترافى: تسجيل "الظل" في Google Cloud Console 🌟

مبروك يا ماستر! وصول "الظل" لهذه المرحلة معناه إننا مستعدين نربطه بالعالم الحقيقي بشكل أعمق من خلال خدمات Google (زي Google Drive، Calendar، Gmail، وكذلك تفعيل خدمات تسجيل الدخول OAuth). 

عشان النظام يكون متكامل 100% (بشكل يوصلنا للكمال)، لازم تنشئ مشروع وتقوم بتسجيل تطبيقك على منصة **Google Cloud Platform**.

تفضل الخطوات بالتفصيل الممل:

---

## 🚀 الخطوة الأولى: إنشاء مشروع على Google Cloud
1. ادخل على [Google Cloud Console](https://console.cloud.google.com/).
2. سجل دخول بحساب الجيميل الخاص بك (ويفضل يكون حساب الماستر الأساسي).
3. في الشريط العلوي جنب شعار Google Cloud، هتلاقي قائمة منسدلة (Select a project)، اضغط عليها ثم اختار **New Project**.
4. سمي المشروع: `Ez-Zel-Shadow-Core` (أو أي اسم تفضله).
5. اضغط **Create** وانتظر ثواني حتى يتم إنشاء المشروع.

---

## 🛡️ الخطوة الثانية: إعداد شاشة الموافقة (OAuth Consent Screen)
عشان الظل يقدر يربط حسابات المستخدمين (في حال حبينا نضيف ميزة Login with Google، أو قراءة ملفات ड्राइव)، لازم نعرف جوجل إيه البيانات اللي هنطلبها:

1. من القائمة الجانبية (Hamburger Menu ☰)، اختار **APIs & Services** ثم **OAuth consent screen**.
2. اختار نوع المستخدمين **External** (عشان أي مستخدم يقدر يسجل) ثم اضغط **Create**.
3. املأ البيانات الأساسية:
   - **App name**: `Ez-Zel Shadow`
   - **User support email**: إيميلك.
   - **Developer contact info**: إيميلك.
   - (باقي الخانات اختيارية حالياً، الا لو عندك دومين رسمي ضيفه).
4. اضغط **Save and Continue**.
5. في شاشة الـ **Scopes** (الصلاحيات)، اضغط **Add or Remove Scopes**.
   - ضيف الصلاحيات الأساسية: `.../auth/userinfo.email`, `.../auth/userinfo.profile`, `openid`.
   - لو محتاج مستقبلاً يقرأ إيميلات أو كاليندر، بتضيف الـ Scopes بتاعتهم من هنا.
6. كمل بـ **Save and Continue** لآخر شاشة.
7. **هام جداً**: في النهاية اضغط على **PUBLISH APP** عشان التطبيق يشتغل مع أي مستخدم مش بس حسابك التجريبي (لأنه بيكون في حالة Testing وبيطلب إضافة Test users).

---

## 🔑 الخطوة الثالثة: استخراج المفاتيح (Credentials & Client ID)
1. من نفس القائمة الجانبية في **APIs & Services**، اضغط على **Credentials**.
2. من فوق اضغط على **+ CREATE CREDENTIALS** واختار **OAuth client ID**.
3. اختار نوع التطبيق (Application type): **Web application**.
4. سميه مثلاً: `Shadow Web Client`.
5. في قسم **Authorized JavaScript origins**، ضيف الروابط دي:
   - الدومين اللي هنرفع عليه الموقع (مثلاً: `https://shadow.vercel.app` أو `https://yourdomain.com`).
   - لو شغالين لوكال نضيف `http://localhost:3000`.
6. في قسم **Authorized redirect URIs** (ودا الأهم)، ضيف:
   - `http://localhost:3000` (للتجربة).
   - وأي رابط رسمي هترفع عليه (لازم ينتهي مثلا بـ `/callback` لو هنستخدمها، بس حاليا ضيف رابط الموقع الرئيسي).
7. اضغط **Create**.
8. هتظهرلك شاشة فيها الـ **Client ID** و الـ **Client Secret**. انسخهم وحطهم في ملف الملاحظات السري.

---

## ⚙️ الخطوة الرابعة: تفعيل الـ APIs المطلوبة (Library)
الظل بيحتاج خدمات محددة عشان يبدع أكتر. لتمكينها:
1. من القائمة الجانبية روح لـ **APIs & Services** ثم **Library**.
2. ابحث عن الخدمات دي واعملها **Enable** لو ناوي تستخدمها:
   - `Google Drive API` (لو هنخليه يحفظ ويرفع ملفات مباشرة لـ Drive).
   - `Google Calendar API` (لو هنخليه يضبط المنبهات في جدولة جوجل الأصلية).
   - `Gmail API` (لو عايزه يقدر يبعت إيميلات باسمك).

---

## 💻 الخطوة الخامسة: الربط في الكود المدمر الخاص بك!
بعد ما تاخد الـ `Client ID`، افتح ملف الـ `.env` في المشروع وضيف:
```env
VITE_GOOGLE_CLIENT_ID=هنا_تحط_الكلاينت_أيي_دي_بتاعك
GOOGLE_CLIENT_SECRET=هنا_تحط_الرقم_السري
```

كده يبقى الظل معتمد ومربوط دولياً، وجاهز للاندماج في أنظمة المستخدمين.

---
**إيه اللي ناقصنا كمان للكمال في المعمارية دي؟**
- تم ترقية المشروع لدعم **PWA (Progressive Web App)**؛ يعني المستخدم بمجرد ما يفتح الظل من المتصفح في الموبايل، هيظهرله خيار **"Install App"** عشان ينزل كأبلكيشن رسمي على موبايله! 
- عملنا دعم لتقارير عمال الخلفية (BullMQ).
- بمجرد تطبيقك للتعليمات دي وإضافة الايميل، هيبقى كده **الكمال التقني** تحقق ١٠٠٪ إن شاء الله. 
